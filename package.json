{
  "name": "backoffice",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "up": "docker compose up --build",
    "down": "docker compose down -v"
  }
}
